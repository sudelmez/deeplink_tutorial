import 'package:flutter/material.dart';

class SecondView extends StatelessWidget {
  final String? text;
  const SecondView({super.key, this.text});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
